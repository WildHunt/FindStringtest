import java.util.Arrays;

//update

public class Input {
    //public String line1;

  //  public String line2;

    public char[] line;


    public Input(char[] line) {
        this.line = line;
    }

    public int check(Input  first, Input second){
        char matrix[][];

        for (int i = 0; i < first.line.length; i++) {
            for (int j = 0; j < first.line[i]; j++) {
               // System.out.print([i][j] + "\t");
                matrix
            }
            System.out.println();
        }
    }


        return temp;
    }








    @Override
    public String toString() {
        return "Input{" +
                "line=" + Arrays.toString(line) +
                '}';
    }
}
